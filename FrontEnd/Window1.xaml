<Window x:Class="FrontEnd.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontEnd"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">

    <Canvas>
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Height="76" Width="204" Canvas.Left="10" Canvas.Top="10" HorizontalAlignment="Left" VerticalAlignment="Center">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
        </ListBox>

        <WrapPanel Canvas.Left="21" Canvas.Top="123" HorizontalAlignment="Left" VerticalAlignment="Center" Width="118">
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
            <TextBlock Text="btn 1"/>
        </WrapPanel>
    </Canvas>
</Window>
