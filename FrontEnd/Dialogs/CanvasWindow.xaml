<Window x:Class="FrontEnd.Dialogs.CanvasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontEnd.Dialogs"
        mc:Ignorable="d"
        Height="450" Width="800" Icon="/Assets/Images/app_icon.ico"
        Background="{StaticResource LLM.Surface}">
    <Window.Resources>
        <ResourceDictionary Source="/Ressources/Styles.xaml"/>
    </Window.Resources>
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="95*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center">
            <TextBlock FontWeight="Bold" Text="Circuit" FontSize="30" Foreground="{StaticResource LLM.TextPrimary}"/>
        </Label>
        <StackPanel Grid.Row="2" Background="{StaticResource LLM.Surface2}" Grid.Column="0">
            <TextBlock Margin="12,12,0,6" Foreground="{StaticResource LLM.TextPrimary}" FontFamily="Cascadia Code" FontSize="14" FontWeight="Bold" Text="MACHINES DISPONIBLES"/>
            <ListBox x:Name="MachineList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Visible"/>
        </StackPanel>
        <Grid Grid.Column="2">
            <Canvas MouseMove="MainCanvas_MouseMove" MouseLeftButtonUp="MainCanvas_MouseLeftButtonUp" x:Name="MainCanvas" Margin="0,10,0,0"/>
            <Button x:Name="Mode_dessin" Click="Mode_dessin_Click" Margin="10,10,0,0" VerticalAlignment="Top">
                <TextBlock Text="MODE DESSIN" Height="Auto"/>
            </Button>
        </Grid>
        
    </Grid>
</Window>
